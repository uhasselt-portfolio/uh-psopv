import React from 'react';

interface PostInterface {
    title: string,
    addres: string,
    sector: string,
    general: string,
    latitude: Number,
    longitude: Number
}

export default PostInterface;